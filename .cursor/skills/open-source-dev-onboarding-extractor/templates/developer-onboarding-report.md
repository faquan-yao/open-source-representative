# 开发者项目概览：{{项目名称}}

> **仓库：** {{仓库 URL}}  
> **提取日期：** {{日期}}  
> **验证：** {{已执行命令 / 仅文档}}

## 摘要

{{3～5 句完整中文。顺序：解决什么问题 → 面向谁 → 技术形态（自然表述，勿写「是一个 X CLI 与库」）→ 关键外部依赖 → 边界。参见 writing-style.md}}

---

## 项目整体架构图

{{2～3 句读图说明：谁触发、主路径、数据与外部依赖}}

```mermaid
flowchart TB
  subgraph 参与者
    Actor[{{角色}}]
  end
  subgraph 核心系统
    Entry[{{入口}}]
    Core[{{核心逻辑}}]
    Store[( {{持久化}} )]
  end
  subgraph 外部
    Ext1[{{外部服务}}]
    Ext2[{{外部服务}}]
  end
  Actor --> Entry --> Core
  Core --> Store
  Core --> Ext1
  Core --> Ext2
```

### 主要组件

| 组件 | 职责 | 入口（可选） |
|------|------|--------------|
| {{名称}} | {{动词短语说明}} | `{{path}}` |
| {{…}} | {{…}} | {{…}} |

---

## 问题域与边界

**解决什么问题**

{{2～3 句，说明痛点与项目回应}}

**主要使用者**

| 角色 | 目标 |
|------|------|
| {{…}} | {{…}} |

**典型场景**

1. {{场景 — 含触发与结果，各 1～2 句}}
2. {{…}}
3. {{…}}

**范围内 / 范围外**

- **做：** {{3～5 条}}
- **不做：** {{2～3 条}}

---

## 主执行链路

**触发方式：** {{如：CLI 子命令、HTTP 路由、消息消费 — 一句}}

{{1 句读图说明：从触发到结果的主路径，与架构图互补（此处强调顺序，不重复组件全景）}}

```mermaid
flowchart LR
  Trigger[{{触发}}]
  S1[{{步骤 1 简述}}]
  S2[{{步骤 2 简述}}]
  S3[{{步骤 3 简述}}]
  S4[{{步骤 4 简述}}]
  S5[{{步骤 5 — 可选}}]
  S6[{{步骤 6 — 可选}}]
  Trigger --> S1 --> S2 --> S3 --> S4 --> S5 --> S6
```

{{4～6 步；节点用动词短语；删去未用步骤节点与连线；有分支时用 `flowchart TB` 或带 `{{条件}}` 的菱形节点}}

| 步骤 | 发生什么 | 关键位置 |
|------|----------|----------|
| 1 | {{…}} | `{{path}}` |
| 2 | {{…}} | `{{path}}` |
| 3 | {{…}} | `{{path}}` |
| 4 | {{…}} | `{{path}}` |
| 5 | {{…}} | `{{path}}` |
| 6 | {{…}} | `{{path}}` |

{{4～6 步；每步只保留 1 个最关键的路径锚点，不写 file:function 全集}}

**改动从哪入手**

- 改入站/触发：从 `{{path}}` 开始
- 改核心逻辑：从 `{{path}}` 开始
- 改存储/配置：从 `{{path}}` 开始

---

## 代码导航

按**能力**定位，不按目录字母序。约 **6～8 行**。

| 能力 | 入口 | 说明 |
|------|------|------|
| {{启动}} | `{{path}}` | {{一行}} |
| {{核心服务}} | `{{path}}` | {{一行}} |
| {{主链路枢纽}} | `{{path}}` | {{一行}} |
| {{配置}} | `{{path}}` | {{一行}} |
| {{扩展/API}} | `{{path}}` | {{一行}} |
| {{测试参考}} | `{{path}}` | {{一行}} |

---

## 配置与运行

### 关键配置

{{配置优先级一句，如：环境变量 > 配置文件 > 默认值}}

| 配置项 | 用途 | 必填 |
|--------|------|------|
| {{键或环境变量}} | {{…}} | {{是/否}} |
| {{…}} | {{…}} | {{…}} |

{{仅列 3～6 个最关键项；其余指向官方文档}}

### 环境与命令

**环境：** {{运行时版本、系统依赖 — 完整句}}

```bash
# 安装
{{INSTALL_CMD}}

# 构建（若需要）
{{BUILD_CMD}}

# 运行
{{RUN_CMD}}
```

**验证记录：** {{已执行命令及结果，或「未验证：原因」}}

### 测试与贡献（摘要）

- **本地测试：** `{{主测试命令}}`
- **贡献入口：** {{CONTRIBUTING 或 PR 流程 — 2～3 步}}

---

## 注意事项

{{遗留模块、文档过时、常见踩坑 — 2～4 条，每条一行}}

- {{…}}
- {{…}}

---

## 延伸阅读

| 文档 / 文件 | 何时查阅 |
|-------------|----------|
| `{{path}}` | {{理由}} |
| `{{…}}` | {{…}} |
| `{{…}}` | {{…}} |
| `{{…}}` | {{…}} |
| `{{…}}` | {{…}} |

共 **5～7** 条。完整配置表、CI 门禁、架构 ADR 等细节请查上表，不展开罗列。
